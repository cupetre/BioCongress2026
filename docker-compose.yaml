services:

  postgres:
    image: postgres:18
    environment:
      POSTGRES_DB: icof
      POSTGRES_USER: icof
      POSTGRES_PASSWORD: icof123
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql

volumes:
  postgres_data: